# PostgreSQL connection string (Neon, Supabase, or local)
DATABASE_URL="postgresql://user:password@localhost:5432/ss_passion_pg"

# Random long string used to sign admin session tokens
AUTH_SECRET="replace-with-a-long-random-string"

# Created via `npm run db:seed` (see README) — change immediately after first login
ADMIN_EMAIL="admin@sspassionpg.com"
ADMIN_PASSWORD="ChangeMe123!"
